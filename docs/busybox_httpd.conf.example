# BusyBox httpd minimal config for Prudynt WebUI via CGI
# Place this file at /etc/httpd.conf and adjust paths as needed

# Serve static files from /www
H:/www

# Enable CGI in /cgi-bin
A:/cgi-bin:/www/cgi-bin

# Map our CGI scripts directory
A:/cgi-bin:/opt/prudynt/scripts/cgi-bin

# Allow CGI execution
# (BusyBox httpd enables CGI for any executable file in a CGI alias)

# Optional: restrict to localhost for control endpoints
#i::1
#i:127.0.0.1

